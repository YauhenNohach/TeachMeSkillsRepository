package Pasha.lesson8.lesson;

public class SavingAccount extends Account{
    protected SavingAccount(double balance, Bank bank, String accountCurrency) {
        super(balance, bank, accountCurrency);
    }
}
